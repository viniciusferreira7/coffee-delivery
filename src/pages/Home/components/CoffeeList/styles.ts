import styled from 'styled-components'

export const CoffeeListContainer = styled.section`
  max-width: 90rem;

  padding: 0 10.5%;

  margin: auto;

  & > header {
    h1 {
      font-size: 2rem;
    }
  }
`
